INHERIT: config.yml
site_name: BeeWare
site_url: https://beeware.org/
docs_dir: it

theme:
  language: it

extra:
  translation_type: machine

plugins:
  macros:
    include_yaml:
        - team: it/news/.authors.yml
